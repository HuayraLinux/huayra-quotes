import {
  quoteRender
} from '../../../helpers/quote-render';
import { module, test } from 'qunit';

module('QuoteRenderHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = quoteRender(42);
  assert.ok(result);
});
